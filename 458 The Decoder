#include<bits/stdc++.h>
using namespace std;
int main()
{
    string sen;
    int ln;
    while(getline(cin,sen))
    {
        ln=sen.size();
        for(int i=0;i<ln;i++)
        {
            printf("%c",sen[i]-7);
        }
        cout<<endl;
    }
    return 0;
}
